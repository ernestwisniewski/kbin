<div class="kbin-magazine-moderator-form mt-lg-4">
    {{ form_start(form) }}
    <div class="input-group-lg input-group-tags mb-4">
        {{ form_label(form.tags, 'tags'|trans) }}
        {{ form_widget(form.tags) }}
    </div>

    <div class="mb-12 text-end">
        {{ form_row(form.submit, { 'label': 'add'|trans }) }}
    </div>
    {{ form_end(form) }}
</div>