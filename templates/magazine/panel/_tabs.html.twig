<ul class="nav nav-tabs mb-3">
    <li class="nav-item">
        <a class="nav-link {{ is_active_route('magazine_edit') ? 'active font-weight-bold' : '' }}" aria-current="page"
           href="{{ path('magazine_edit', {'name': magazine.name}) }}">Ogólne</a>
    </li>

    <li class="nav-item">
        <a class="nav-link {{ is_active_route('magazine_appearance') ? 'active font-weight-bold' : '' }}" aria-current="page"
           href="{{ path('magazine_edit', {'name': magazine.name}) }}">Wygląd</a>
    </li>

    <li class="nav-item">
        <a class="nav-link {{ is_active_route('magazine_stats') ? 'active font-weight-bold' : '' }}" aria-current="page"
           href="{{ path('magazine_edit', {'name': magazine.name}) }}">Statystyki</a>
    </li>

    <li class="nav-item">
        <a class="nav-link {{ is_active_route('magazine_moderators') ? 'active font-weight-bold' : '' }}" aria-current="page"
           href="{{ path('magazine_edit', {'name': magazine.name}) }}">Moderatorzy</a>
    </li>

    <li class="nav-item">
        <a class="nav-link {{ is_active_route('magazine_bans') ? 'active font-weight-bold' : '' }}" aria-current="page"
           href="{{ path('magazine_edit', {'name': magazine.name}) }}">Bany</a>
    </li>

    <li class="nav-item">
        <a class="nav-link {{ is_active_route('magazine_trash') ? 'active font-weight-bold' : '' }}" aria-current="page"
           href="{{ path('magazine_edit', {'name': magazine.name}) }}">Kosz</a>
    </li>
</ul>
