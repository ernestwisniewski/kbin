<h2 class="h5 mb-2">
    <a class="text-decoration-none text-reset font-weight-bold"
          href="{{ path('front_magazine', {name: magazine.name}) }}">{{ magazine.title }}</a>
</h2>
<div class="hr"></div>
