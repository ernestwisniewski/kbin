{% extends 'base.html.twig' %}

{% block title %}{{ 'stats'|trans }}{% endblock %}

{% block body %}
    <div class="kbin-stats">
        <h1 class="font-weight-bold mb-5">{{ 'stats'|trans }}</h1>

        <div>
            {{ render_chart(contentChart) }}
        </div>
    </div>
{% endblock %}
