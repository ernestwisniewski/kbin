{% use 'vote/_vote.html.twig' %}

{%- macro vote(votable, options) -%}
    {%- with {
        votable: votable,
        self: app.user,
        hide_downvote: options.hide_downvote ?? null,
        form_dest: options.form_dest ?? null,
        base_class: options.base_class ?? 'entry',
        notification_type: options.notification_type ?? 'None',
        } only -%}
        {{ block('vote') }}
    {%- endwith -%}
{%- endmacro -%}
