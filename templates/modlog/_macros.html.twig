{% use 'modlog/_modlog.html.twig' %}

{%- macro modlog(logs, options) -%}
    {%- with {
        logs: logs,
        show_magazine: options.show_magazine ?? null
        } only -%}
        {{ block('modlog') }}
    {%- endwith -%}
{%- endmacro -%}
