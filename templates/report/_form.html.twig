{{ form_start(form) }}
<div class="kbin-magazine-create-form my-3">
    <div class="mb-3">
        {{ form_label(form.reason, 'Powód') }}
        <div class="input-group">
            {{ form_widget(form.reason) }}
        </div>
    </div>

    <div class="mb-12 text-end">
        {{ form_row(form.submit, { 'label': 'Gotowe' }) }}
    </div>
</div>
{{ form_end(form) }}
