<ul class="nav nav-tabs justify-content-end mb-2">
    {% if is_comments_page() or is_posts_page() %}
        <li class=" kbin-nav-navbar-item">
            <a class="nav-link text-decoration-none text-muted {{ is_active_sort_option('wazne', false) ? 'active' : '' }}"
               href="{{ get_active_sort_option_path('wazne', get_active_time_option(), false) }}">Najlepsze</a>
        </li>
        <li class=" kbin-nav-navbar-item">
            <a class="nav-link text-decoration-none text-muted {{ is_active_sort_option('najnowsze', false) ? 'active' : '' }}"
               href="{{ get_active_sort_option_path('najnowsze', get_active_time_option(), false) }}">Najnowsze</a>
        </li>
        <li class=" kbin-nav-navbar-item">
            <a class="nav-link text-decoration-none text-muted {{ is_active_sort_option('aktywne', false) ? 'active' : '' }}"
               href="{{ get_active_sort_option_path('aktywne', get_active_time_option(), false) }}">Aktywne</a>
        </li>
    {% endif %}
    <li class="nav-item">
        <div class="dropdown">
            <a class="nav-link active text-decoration-none text-muted"
               aria-current="page"
               id="kbin-time-filter"
               data-bs-toggle="dropdown"
               aria-expanded="false" href="#">
                <i class="fas fa-history"></i> {{ get_active_time_option() }}</a>
            <ul class="dropdown-menu" aria-labelledby="kbin-time-filter">
                <li><a class="dropdown-item" href="{{ get_active_sort_option_path(get_active_sort_option(), '6h', false) }}">6h</a></li>
                <li><a class="dropdown-item" href="{{ get_active_sort_option_path(get_active_sort_option(), '12h', false) }}">12h</a></li>
                <li><a class="dropdown-item" href="{{ get_active_sort_option_path(get_active_sort_option(), '1d', false) }}">24h</a></li>
                <li><a class="dropdown-item" href="{{ get_active_sort_option_path(get_active_sort_option(), '1w', false) }}">tydzień</a></li>
                <li><a class="dropdown-item" href="{{ get_active_sort_option_path(get_active_sort_option(), '1m', false) }}">miesiąc</a></li>
                <li><a class="dropdown-item" href="{{ get_active_sort_option_path(get_active_sort_option(), '1y', false) }}">rok</a></li>
                <li><a class="dropdown-item" href="{{ get_active_sort_option_path(get_active_sort_option(), '∞', false) }}">∞</a></li>
            </ul>
        </div>
    </li>
    {% if not is_comments_page() and not is_posts_page() %}
        <li class="nav-item">
            <a class="nav-link active text-decoration-none text-muted"
               aria-current="page"
               id="kbin-type-filter"
               data-bs-toggle="dropdown"
               aria-expanded="false" href="#">
                <i class="fas fa-photo-video"></i> wszystko</a>
            <ul class="dropdown-menu" aria-labelledby="kbin-type-filter">
                <li><a class="dropdown-item" href="#">artykuły</a></li>
                <li><a class="dropdown-item" href="#">obrazy</a></li>
                <li><a class="dropdown-item" href="#">video</a></li>
            </ul>
        </li>
    {% endif %}
    <li class="nav-item">
        <a data-action="kbin#dark" class="nav-link active text-decoration-none text-muted" aria-current="page" href="#"><i
                    class="far fa-lightbulb"></i></a>
    </li>
</ul>
