<ul class="nav nav-tabs justify-content-end mb-2">
    {% if is_comments_page() or is_posts_page() %}
        <li class=" kbin-nav-navbar-item">
            <a class="nav-link text-decoration-none text-muted {{ is_active_sort_option('wazne', false) ? 'active' : '' }}"
               href="{{ get_active_sort_option_path('wazne', get_active_time_option(), get_active_type_option(), false) }}">Najlepsze</a>
        </li>
        <li class=" kbin-nav-navbar-item">
            <a class="nav-link text-decoration-none text-muted {{ is_active_sort_option('najnowsze', false) ? 'active' : '' }}"
               href="{{ get_active_sort_option_path('najnowsze', get_active_time_option(), get_active_type_option(), false) }}">Najnowsze</a>
        </li>
        <li class=" kbin-nav-navbar-item">
            <a class="nav-link text-decoration-none text-muted {{ is_active_sort_option('aktywne', false) ? 'active' : '' }}"
               href="{{ get_active_sort_option_path('aktywne', get_active_time_option(), get_active_type_option(), false) }}">Aktywne</a>
        </li>
    {% endif %}
    <li class="nav-item">
        <div class="dropdown">
            <a class="nav-link active text-decoration-none text-muted"
               aria-current="page"
               id="kbin-time-filter"
               data-bs-toggle="dropdown"
               aria-expanded="false" href="#">
                <i class="fas fa-history"></i> {{ get_active_time_option() }}</a>
            <ul class="dropdown-menu" aria-labelledby="kbin-time-filter">
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), '6h', get_active_type_option(), false) }}">6h</a></li>
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), '12h', get_active_type_option(), false) }}">12h</a></li>
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), '1d', get_active_type_option(), false) }}">24h</a></li>
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), '1w', get_active_type_option(), false) }}">tydzień</a></li>
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), '1m', get_active_type_option(), false) }}">miesiąc</a></li>
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), '1y', get_active_type_option(), false) }}">rok</a></li>
                <li><a class="dropdown-item" href="{{ get_active_sort_option_path(get_active_sort_option(), '∞', get_active_type_option(), false) }}">∞</a>
                </li>
            </ul>
        </div>
    </li>
    {% if not is_comments_page() and not is_posts_page() %}
        <li class="nav-item">
            <a class="nav-link active text-decoration-none text-muted"
               aria-current="page"
               id="kbin-type-filter"
               data-bs-toggle="dropdown"
               aria-expanded="false" href="#">
                <i class="fas fa-photo-video"></i> {{ get_active_type_option() ?get_active_type_option() : 'wszystko' }}</a>
            <ul class="dropdown-menu" aria-labelledby="kbin-type-filter">
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), get_active_time_option(), 'link', false) }}">link</a></li>
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), get_active_time_option(), 'artykul', false) }}">artykuł</a></li>
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), get_active_time_option(), 'foto', false) }}">foto</a></li>
                <li><a class="dropdown-item"
                       href="{{ get_active_sort_option_path(get_active_sort_option(), get_active_time_option(), 'video', false) }}">video</a></li>
            </ul>
        </li>
    {% endif %}
    <li class="nav-item">
        <a data-action="kbin#toggleTheme" class="nav-link active text-decoration-none text-muted" aria-current="page"
           href="{{ path('user_profile_toggle_theme') }}"><i
                    class="far fa-lightbulb"></i></a>
    </li>
</ul>
