<ul class="nav nav-pills mt-3 mb-1 small">
    <li class="nav-item small">
        <a class="nav-link {{ is_active_route('domain_front') ? 'active' : '' }}" aria-current="page" href="{{ get_active_sort_option_path('sort.hot'|trans|lower) }}">{{ 'threads'|trans }}</a>
    </li>
    <li class="nav-item small">
        <a class="nav-link {{ is_active_route('domain_entry_comments_front') ? 'active' : '' }}" aria-current="page" href="{{ get_active_comments_page_path() }}">{{ 'comments'|trans }}</a>
    </li>
</ul>