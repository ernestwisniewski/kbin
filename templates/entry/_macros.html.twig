{% use 'entry/_entry.html.twig' %}

{%- macro entry(entry, options) -%}
    {%- with {
        entry: entry,
        self: app.user,
        extra_classes: options.extra_classes ?? null,
        show_content: options.show_content ?? null,
        } only -%}
        {{ block('entry') }}
    {%- endwith -%}
{%- endmacro -%}
