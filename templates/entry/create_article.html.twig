{% extends 'base.html.twig' %}

{%- block title -%}
    {{- 'add_new_article'|trans }} - {{ parent() -}}
{%- endblock -%}

{% block mainClass %}page-entry-create page-entry-create-link{% endblock %}

{% block header_nav %}
{% endblock %}

{% block sidebar_top %}
{% endblock %}

{% block body %}
    {% include 'entry/_create_options.html.twig' %}

    <div id="content" class="section">
        <div class="container">
            {{ form_start(form) }}
            <h1>{{ 'add_new_article'|trans }}</h1>
            {{ form_row(form.title, {label: 'title'}) }}
            {{ form_row(form.body, {label: 'body'}) }}
            {{ form_row(form.magazine, {label: 'magazine'}) }}
            {{ form_row(form.tags, {label: 'tags'}) }}
            {{ form_row(form.badges, {label: 'badges'}) }}
            <div class="group params">
                {{ form_row(form.isAdult, {label: 'is_adult', row_attr: {class: 'checkbox'}}) }}
                {{ form_row(form.isEng, {label: 'eng', row_attr: {class: 'checkbox'}}) }}
                {{ form_row(form.isOc, {label: 'oc', row_attr: {class: 'checkbox'}}) }}
            </div>
            {{ form_row(form.submit, {label: 'add_new_article', attr: {class: 'btn btn__primary'}, row_attr: {class: 'float-end'}}) }}

            <div hidden class="media">
                {{ form_row(form.image, {label: 'image'}) }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
