{% extends 'base.html.twig' %}

{%- block title -%}
    {{- 'add_comment'|trans }} - {{ entry.title }} - {{ parent() -}}
{%- endblock -%}

{% block mainClass %}page-entry-comment-create{% endblock %}

{% block header_nav %}
{% endblock %}

{% block sidebar_top %}
{% endblock %}

{% block body %}
    {{ component('entry', {entry: entry, isSingle: true, showShortSentence: false, showBody:false}) }}
    {% if parent is defined and parent %}
        {{ component('entry_comment', {comment: parent, showEntryTitle: false, showNested: false}) }}
    {% endif %}
    <section id="content" class="section">
        {% include 'entry/comment/_form_comment.html.twig' %}
    </section>
{% endblock %}
