{{ form_start(form) }}
<div class="kbin-comment-create-form mt-3">
    <div class="mb-3">
        {{ form_widget(form.body) }}
    </div>

    <div class="mb-12 text-end">
        {{ form_row(form.submit, { 'label': 'Gotowe' }) }}
    </div>
</div>
{{ form_end(form) }}
