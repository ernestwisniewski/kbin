{% from 'comment/_macros.html.twig' import comment %}

<div class="kbin-comment-list mt-3">
    {% for comment in comments %}
        {{ comment(comment, {
            comment: comment,
            self: app.user,
            extra_classes: 'kbin-comment-list-item'
        }) }}
    {% endfor %}
</div>
