<figure{{ attributes }}>
    <img width="{{ this.width }}" height="{{ this.height }}"
         src="{{ asset(this.user.avatar.filePath) | imagine_filter('avatar_thumb') }}"
         alt="{{ this.user.username ~' '~ 'avatar'|trans|lower }}">
</figure>
