<figure{{ attributes }}>
    {% if asLink %}
    <a data-action="mouseover->kbin#mention" data-kbin-username-param="{{ mention_url(user.username) }}"
       href="{{ path('user_overview', {username: user.username}) }}">
        {% endif %}
        <img width="{{ width }}" height="{{ height }}"
             style="max-width: {{ width }}px; max-height: {{ height }}px;"
             src="{{ user.avatar ? (asset(user.avatar.filePath) | imagine_filter('avatar_thumb')) : '' }}"
             alt="{{ user.username ~' '~ 'avatar'|trans|lower }}">
        {% if asLink %}
    </a>
    {% endif %}
</figure>
