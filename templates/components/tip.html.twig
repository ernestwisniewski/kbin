{% from '_layout/_not_found.html.twig' import not_found_transactions %}

<div class="kbin-tips-page mt-3">
    <div class="kbin-profile-tips mt-lg-4">
        <button type="button" class="btn-close mb-3" data-tip-target="close" aria-label="Zamknij"
                data-action="tip#close"></button>
        <div class="col-lg-6">
            <div class="kbin-cardano-background"></div>

            <ul class="nav nav-pills mb-3" id="pills-tab" role="tablist">
                <li class="nav-item" role="presentation">
                    <button class="nav-link active" id="kbin-{{ this.key ~ '-' ~ this.subject.id }}-home-tab" data-bs-toggle="pill" data-bs-target="#kbin-{{ this.key ~ '-' ~ this.subject.id }}-home" type="button"
                            role="tab" aria-controls="kbin-{{ this.key ~ '-' ~ this.subject.id }}-home" aria-selected="true"><small>{{ 'external_transfer'|trans }}</small>
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="kbin-{{ this.key ~ '-' ~ this.subject.id }}-profile-tab" data-bs-toggle="pill" data-bs-target="#kbin-{{ this.key ~ '-' ~ this.subject.id }}-profile" type="button"
                            role="tab" aria-controls="kbin-{{ this.key ~ '-' ~ this.subject.id }}-profile" aria-selected="false"><small>{{ 'mnemonic'|trans }}</small>
                    </button>
                </li>
                <li class="nav-item" role="presentation">
                    <button class="nav-link" id="kbin-{{ this.key ~ '-' ~ this.subject.id }}-contact-tab" data-bs-toggle="pill" data-bs-target="#kbin-{{ this.key ~ '-' ~ this.subject.id }}-contact" type="button"
                            role="tab" aria-controls="kbin-{{ this.key ~ '-' ~ this.subject.id }}-contact" aria-selected="false"><small>{{ 'hardware_wallet'|trans }}</small>
                    </button>
                </li>
            </ul>

            <div class="tab-content clearfix" id="kbin-tab-content">
                <div class="tab-pane fade show active my-5" id="kbin-{{ this.key ~ '-' ~ this.subject.id }}-home" role="tabpanel" aria-labelledby="kbin-{{ this.key ~ '-' ~ this.subject.id }}-home-tab">
                    <label for="copyWallet">{{ 'wallet_address'|trans }}</label>
                    <div class="input-group input-group-xl"
                         data-controller="clipboard"
                         data-clipboard-success-content="Skopiowano!">
                        <input type="text"
                               id="{{ this.key ~ '-' ~ this.subject.id }}-copy-wallet-button"
                               class="form-control"
                               aria-label="Skopiuj"
                               aria-describedby="Skopiuj ID portfela"
                               data-clipboard-target="source"
                               value="{{ this.subject.user.cardanoWalletId }}">
                        <button class="input-group-text" id="{{ this.key ~ '-' ~ this.subject.id }}-copy-wallet-button" data-action="clipboard#copy"
                                data-clipboard-target="button">{{ 'copy'|trans }}</button>
                    </div>

                    <p class="mt-5">
                        {{ 'dont_have_wallet_yet'|trans({'%link_start%' : '<a class="font-weight-bold" href="#"  data-bs-toggle="modal" data-bs-target="#cardanoWallet">', '%link_end%' : '</a>'}) | raw }}
                    </p>

                    <ul class="ms-3">
                        <li><a href="#">{{ 'tip_link_1'|trans }}</a></li>
                        <li><a href="#">{{ 'tip_link_2'|trans }}</a> <small>({{ 'recommended'|trans }})</small></li>
                        <li><a href="#">{{ 'tip_link_3'|trans }}</a></li>
                        <li><a href="#">{{ 'tip_link_4'|trans }}</a></li>
                        <li><a href="#">{{ 'tip_link_5'|trans }}</a></li>
                    </ul>

                    <div class="modal" tabindex="-1" data-bs-toggle="modal" id="cardanoWallet">
                        <div class="modal-dialog modal-dialog-centered modal-xl">
                            <div class="modal-content">
                                <div class="modal-header">
                                    <h5 class="modal-title">{{ 'create_new_wallet'|trans }}</h5>
                                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                </div>
                                <div class="modal-body">
                                    <p>{{ 'create_new_wallet_desc'|trans|raw }}</p>

                                    <div class="my-5 input-group-lg">
                                        <input class="form-control" placeholder="coming soon...">
                                    </div>
                                </div>
                                <div class="modal-footer">
                                    <button type="button" class="btn btn-primary">{{ 'create_new_wallet_apply'|trans }}</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="tab-pane fade" id="kbin-{{ this.key ~ '-' ~ this.subject.id }}-profile" role="tabpanel" aria-labelledby="kbin-{{ this.key ~ '-' ~ this.subject.id }}-profile-tab">
                    <div class="alert alert-primary" role="alert">
                        <small>Here you can use your mnemonic to access your wallet.</small>
                    </div>

                    <div class="my-5  input-group-lg">
                        <label for="{{ this.key ~ '-' ~ this.subject.id }}-cardano-wallet" class="form-label"><small>Enter the 12, 15, 24 or 27-word wallet mnemonic seed
                                phrase</small></label>
                        <input class="form-control" id="{{ this.key ~ '-' ~ this.subject.id }}-cardano-wallet" placeholder="coming soon...">
                        <button type="submit" class="btn btn-primary float-end my-3">Unlock</button>

                    </div>
                </div>
                <div class="tab-pane fade" id="kbin-{{ this.key ~ '-' ~ this.subject.id }}-contact" role="tabpanel" aria-labelledby="kbin-{{ this.key ~ '-' ~ this.subject.id }}-contact-tab">
                    <div class="container text-center my-5">
                        coming soon...
                    </div>
                </div>
            </div>

            <hr class="my-4">
        </div>
    </div>
    <div class="kbin-profile-transactions mt-lg-4">
        <h2 class="font-weight-bold">{{ 'transactions'|trans }}</h2>
        {% if this.transactions|length %}
            <div class="table-responsive">
                <table class="table align-middle small">
                    <tbody>
                    {% for transaction in this.transactions %}

                    {% endfor %}
                    </tbody>
                </table>
            </div>

            {# {% if(threads.haveToPaginate) %} #}
            {# {{ pagerfanta(transactions, null, {'pageParameter':'[p]'}) }} #}
            {# {% endif %} #}
        {% else %}
            {{ not_found_transactions() }}
        {% endif %}
        <hr>

    </div>
</div>
