{% for reply in comment.children %}
    {% if not app.user or (app.user and not app.user.isBlocked(reply.user)) %}
        {{ component('post_comment', {comment: reply, showNested:true, level: level + 1, showMagazineName:false}) }}
    {% endif %}
{% endfor %}