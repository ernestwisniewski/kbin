{% if categories|length %}
    <section{{ attributes.defaults({class: 'magazines section related-magazines'}) }}>
        <h3>{{ title|trans }}</h3>
        <div class="container">
            <ul class="meta">
                {% for category in categories %}
                    <li>
                        <a href="{{ category_url(category) }}" class="stretched-link">{{ category.name }}</a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </section>
{% endif %}