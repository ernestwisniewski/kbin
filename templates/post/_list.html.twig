{% for post in posts %}
    {{ component('post', {post: post, showMagazineName: magazine is not defined or not magazine}) }}
{% endfor %}

{% if not posts|length %}
    <aside class="section section--muted user-list">
        <p>{{ 'empty'|trans }}</p>
    </aside>
{% endif %}
