{% extends 'base.html.twig' %}

{%- block title -%}{{- 'favourites'|trans|capitalize }} - {{ get_short_desc(post.body) -}}{%- endblock -%}

{% block body %}
    <section class="kbin-post-wrapper mt-4 mb-4">
        {{ component('post', {post: post}) }}
    </section>

    {{ component('title', {title: 'favourites'|trans|capitalize }) }}
    {% include '_layout/_favourites_list_grid.html.twig' with { subject: post } %}
{% endblock %}

{% block sidebar %}
    {% include 'post/_sidebar.html.twig' %}

    {{ parent() }}

    {% include 'magazine/_sidebar.html.twig' %}
{% endblock %}
