<aside class="options" id="options">
    <div></div>
    <menu class="options__main">
        <li>
            <a href="{{ options_url('sortBy', 'top'|trans|lower) }}"
               class="{{ html_classes({'active': route_has_param('sortBy', 'top'|trans|lower)}) }}">
                {{ 'top'|trans|lower }}
            </a>
        </li>
        <li>
            <a href="{{ options_url('sortBy', 'hot'|trans|lower) }}"
               class="{{ html_classes({'active': route_has_param('sortBy', 'hot'|trans|lower)}) }}">
                {{ 'hot'|trans|lower }}
            </a>
        </li>
        <li>
            <a href="{{ options_url('sortBy', 'newest'|trans|lower) }}"
               class="{{ html_classes({'active': route_has_param('sortBy', 'newest'|trans|lower)}) }}">
                {{ 'newest'|trans|lower }}
            </a>
        </li>
        <li>
            <a href="{{ options_url('sortBy', 'active'|trans|lower) }}"
               class="{{ html_classes({'active': route_has_param('sortBy', 'active'|trans|lower)}) }}">
                {{ 'active'|trans|lower }}
            </a>
        </li>
        <li>
            <a href="{{ options_url('sortBy', 'commented'|trans|lower) }}"
               class="{{ html_classes({'active': route_has_param('sortBy', 'commented'|trans|lower)}) }}">
                {{ 'commented'|trans|lower }}
            </a>
        </li>
    </menu>
    <menu class="options__filters">
        <li class="dropdown">
            <button aria-label="{{ 'filter_by_time'|trans }}"
                    title="{{ 'filter_by_time'|trans }}"><i
                        class="fa-solid fa-clock-rotate-left"></i> {{ 'filter_by_time'|trans }}</button>
            <ul class="dropdown__menu">
                <li>
                    <a href="{{ options_url('time', '∞') }}"
                       class="{{ html_classes({'active': route_has_param('time', '∞') or not get_route_param('time')}) }}">
                        {{ '∞' }}
                    </a>
                </li>
                <li>
                    <a href="{{ options_url('time', '3h'|trans|lower) }}"
                       class="{{ html_classes({'active': route_has_param('time', '3h'|trans|lower)}) }}">
                        {{ '3h'|trans|lower }}
                    </a></li>
                <li>
                    <a href="{{ options_url('time', '6h'|trans|lower) }}"
                       class="{{ html_classes({'active': route_has_param('time', '6h'|trans|lower)}) }}">
                        {{ '6h'|trans|lower }}
                    </a>
                </li>
                <li>
                    <a href="{{ options_url('time', '12h'|trans|lower) }}"
                       class="{{ html_classes({'active': route_has_param('time', '12h'|trans|lower)}) }}">
                        {{ '12h'|trans|lower }}
                    </a>
                </li>
                {% if get_active_sort_option() is not same as 'sort.hot'|trans|lower %}
                    <li>
                        <a href="{{ options_url('time', '1d'|trans|lower) }}"
                           class="{{ html_classes({'active': route_has_param('time', '1d'|trans|lower)}) }}">
                            {{ '1d'|trans|lower }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ options_url('time', '1w'|trans|lower) }}"
                           class="{{ html_classes({'active': route_has_param('time', '1w'|trans|lower)}) }}">
                            {{ '1w'|trans|lower }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ options_url('time', '1m'|trans|lower) }}"
                           class="{{ html_classes({'active': route_has_param('time', '1m'|trans|lower)}) }}">
                            {{ '1m'|trans|lower }}
                        </a>
                    </li>
                    <li>
                        <a href="{{ options_url('time', '1y'|trans|lower) }}"
                           class="{{ html_classes({'active': route_has_param('time', '1y'|trans|lower)}) }}">
                            {{ '1y'|trans|lower }}
                        </a>
                    </li>
                {% endif %}
            </ul>
        </li>
    </menu>
</aside>
