{% from 'magazine/_macros.html.twig' import magazine %}
{% from 'user/_macros.html.twig' import user %}

{{ user(notification.user) }} dodał/a nowy wpis w {{ magazine(notification.post.magazine) }} - <strong><a
            href="{{ path('post_single', {'magazine_name': notification.post.magazine.name, 'post_id': notification.post.id}) }}">{{ notification.post.shortTitle }}</a></strong>
