{% from 'magazine/_macros.html.twig' import magazine %}

Moderator twój komentarz treść w {{ magazine(notification.comment.magazine) }} - <strong><a
            href="{{ path('entry_single', {'magazine_name': notification.comment.entry.magazine.name, 'entry_id': notification.comment.entry.id}) }}">{{ notification.comment.shortTitle }}</a></strong>
