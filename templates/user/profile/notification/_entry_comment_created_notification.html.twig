{% from 'user/_macros.html.twig' import user %}
{% from '_layout/_macros.html.twig' import entry_url %}

{{ user(notification.user) }} {{ 'added_new_comment'|trans }} {{ 'in'|trans }} {{ component('magazine_inline', { magazine: notification.comment.entry.magazine }) }} -
<strong><a href="{{ entry_url(notification.comment.entry) }}">{{ notification.comment.shortTitle }}</a></strong>
