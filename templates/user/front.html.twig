{% extends 'base.html.twig' %}

{% block title %}{{ user.username }} | Przegląd | {{ parent() }}{% endblock %}

{% block body %}
    <div class="kbin-user-front-page kbin-entry-list kbin-comment-wrapper kbin-post-wrapper">
        {% for subject in results %}
            {% include '_layout/_subject.html.twig' with {subject: subject} %}
        {% endfor %}

        {% if(results.haveToPaginate) %}
            {{ pagerfanta(results, null, {'pageParameter':'[strona]'}) }}
        {% endif %}
    </div>
{% endblock %}

{% block sidebar %}
    {% include 'user/_sidebar.html.twig' %}
    {{ parent() }}
    <div class="mb-4">
        {% include 'user/_moderator_list_card.html.twig' %}
    </div>
{% endblock %}
