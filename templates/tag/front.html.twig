{% extends 'base.html.twig' %}

{%- block title -%}
    #{{- tag ~ (is_active_sort_option('sort.hot'|trans|lower, true) ? '' : ' - ' ~ get_active_sort_option()|capitalize) -}}
{%- endblock -%}

{% block body %}
    <div class="kbin-front-page">
        {% include '_layout/_filters.html.twig' %}
        {% include 'entry/_list.html.twig' %}
    </div>
{% endblock %}

{% block sidebar %}
    {% include 'tag/_sidebar.html.twig' %}

    {{ parent() }}

    <div class="mb-5">
        {{ component('related_posts_sidebar', {tag: tag}) }}
        {{ component('related_entries_sidebar', {tag: tag}) }}
    </div>
{% endblock %}
