tag_overview:
  controller: App\Controller\Tag\TagOverviewController
  path: /tag/{name}
  methods: [GET]

tag_entries:
  controller: App\Controller\Tag\TagEntryFrontController
  defaults: { sortBy: hot, time: ~ }
  path:
    en: tag/{name}/threads/{sortBy}/{time}
    pl: tag/{name}/treści/{sortBy}/{time}
  methods: [ GET ]
  requirements: { sortBy: "%front_sort_options%" }

tag_comments:
  controller: App\Controller\Tag\TagCommentFrontController
  defaults: { sortBy: hot, time: ~ }
  path:
    en: tag/{name}/comments/{sortBy}/{time}
    pl: tag/{name}/komentarze/{sortBy}/{time}
  methods: [GET]
  requirements: { sortBy: "%front_sort_options%" }

tag_posts:
  controller: App\Controller\Tag\TagPostFrontController
  defaults: { sortBy: hot, time: ~ }
  path:
    en: tag/{name}/posts/{sortBy}/{time}
    pl: tag/{name}/wpisy/{sortBy}/{time}
  methods: [GET]
  requirements: { sortBy: "%front_sort_options%" }

tag_people:
  controller: App\Controller\Tag\TagPeopleFrontController
  defaults: { sortBy: hot, time: ~ }
  path:
    en: tag/{name}/people
    pl: tag/{name}/ludzie
  methods: [GET]
  requirements: { sortBy: "%front_sort_options%" }
